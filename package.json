{
  "name": "ytaudioselector",
  "version": "0.9.3",
  "main": "background.js",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.0.312",
    "bestzip": "^2.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "authors": "Bloodiko",
  "description": "Automatically select a predefined audio track on YouTube videos, if audio selection is available.",
  "private": true,
  "scripts": {
    "apply_version": "bun run scripts/apply_version.ts",
    "finalize": "bun run apply_version && bun run scripts/package.ts && bestzip ytaudioselector.zip dist/*"
  },
  "dependencies": {
    "@cospired/i18n-iso-languages": "^4.2.0"
  }
}